import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import * as Table from './Table.stories';

<Meta title="Utilities/Table" />

# Table Utilities

This section showcases the table utilities available in the design system for creating responsive and accessible data tables.

## Basic Table

<Canvas of={Table.BasicTable} />

### Code Example

```html
<div className="table-responsive">
  <table className="table" role="table" aria-label="Data table">
    <thead>
      <tr>
        <th scope="col">Column Header</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Table data</td>
      </tr>
    </tbody>
  </table>
</div>
```

## Table with Hover

<Canvas of={Table.TableWithHover} />

### Code Example

```html
<table className="table table-hover">
  <!-- Table content -->
</table>
```

## Striped Table

<Canvas of={Table.StripedTable} />

### Code Example

```html
<table className="table table-striped">
  <!-- Alternating row colors -->
</table>
```

## Responsive Stacked Table

<Canvas of={Table.ResponsiveStackedTable} />

### Code Example

```html
<table className="table table-stacked">
  <tbody>
    <tr>
      <td data-label="Column Name">Data</td>
      <!-- data-label provides labels for mobile stacked layout -->
    </tr>
  </tbody>
</table>
```

## Table with Caption

<Canvas of={Table.TableWithCaption} />

### Code Example

```html
<table className="table">
  <caption>Table description for screen readers</caption>
  <!-- Table content -->
</table>
```

## Complex Table Example

<Canvas of={Table.ComplexTable} />

## Real-world Examples

<Canvas of={Table.DataTable} />

<Canvas of={Table.ComparisonTable} />

## Best Practices

- Always use `table-responsive` wrapper for horizontal scrolling on small screens
- Include proper semantic HTML with `th` elements and `scope` attributes
- Add `role="table"` and `aria-label` for screen reader accessibility
- Use `caption` element to describe table purpose
- Combine table classes for enhanced functionality (hover, striped)
- For mobile, use `table-stacked` with `data-label` attributes

## Accessibility Notes

- Use semantic table markup (`table`, `thead`, `tbody`, `th`, `td`)
- Add `scope="col"` to column headers and `scope="row"` to row headers
- Include `caption` or `aria-label` to describe table purpose
- Ensure sufficient color contrast for table content
- Use `data-label` attributes for stacked mobile layout
- Maintain keyboard navigation and focus management
- Test with screen readers for proper table navigation
- Avoid using tables for layout purposes